import {atom} from "recoil"

export const yesAtom = atom({
    key: "yesatom",
    default : 0
})

export const noAtom = atom({
    key: "noatom",
    default : 0
})

export const totalAtom = atom({
    key: "totalatom",
    default : 0
})